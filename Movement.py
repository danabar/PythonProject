class Movement:
 def __init__(self, movementTime, fromLocation,toLocation,productId , qty):
  self.movementTime = movementTime
  self.fromLocation = fromLocation
  self.toLocation = toLocation
  self.productId = productId
  self.qty = gty